.DS_Store
__pycache__/
*.pyc

# Generated training artifacts — do not track
/results/checkpoints/
/results/logs/
/results/figures/
/results/bench/
/results/bench_adam/
/results/bench_final/
/results/archive/
/results/balanced/

# Old root-level output
/pressure_front_torch.png

# Local agent/tooling artifacts
/.claude/
/.cursor/
/ref/
/.codegraph/
/.codegrapg/
/docs/
/CLAUDE.md

# --- Track only lightweight evidence under results/ (logs / CSVs / *_history pickles) ---
# Heavy artifacts (checkpoints, plots, animations, papers) stay untracked.
results/**/*.pt
results/**/*.png
results/**/*.gif
results/**/*.pdf
# Heavy steady-sweep run pickles (loss/weight history) — regenerate locally
results/experiments/steady_adaptive_sweep/**/*.pickle
*.pid
# Reference media under data/ is not tracked (keep .mat / .py only)
data/**/*.png
data/**/*.gif

# Local scratch / GPU-pull mirrors / temp
/remote_gpu_pull/
/tmp/

# Superseded by final_report/, and agent tooling (mirrors ignored CLAUDE.md)
/report/
/AGENT.md

# Extra human-obstacle reference data — large; regenerate via the gen_*.py generators
data/reference/unsteady_reference_human_rot90left.mat
data/reference/unsteady_reference_human_rot90left_re100.mat

# Stray local artifacts
/Screenshot*.png
**/logs/pid

# Report figures: track only the three referenced by final.tex
final_report/figures/*.png
!final_report/figures/causal_weight_curve_both.png
!final_report/figures/field_comparison_eps30_uniform_t0p50.png
!final_report/figures/loss_curves.png
