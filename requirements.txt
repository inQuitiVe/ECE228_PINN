torch>=2.4.0
numpy>=1.26.0
scipy>=1.12.0
matplotlib>=3.8.0
pyDOE2>=1.3.0
Pillow>=10.0.0

# After pip install, create pyDOE shim (RunPod installs pyDOE2 but code imports pyDOE):
#   python3 -c "import sys; open(f'/usr/local/lib/python{sys.version_info.major}.{sys.version_info.minor}/dist-packages/pyDOE.py','w').write('from pyDOE2 import *\n')"
