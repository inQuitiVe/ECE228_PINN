# Agent Instructions

Before doing any work in this repository, read `CLAUDE.md` in this same
directory.

`CLAUDE.md` is the mandatory project instruction source for this workspace. Treat
its rules, goals, workflow constraints, and project context as binding unless the
user explicitly gives a newer instruction that overrides a specific item.

If an instruction in this file appears incomplete or ambiguous, defer to
`CLAUDE.md`.
